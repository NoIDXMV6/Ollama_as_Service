# `check_update.ps1`

## Описание
Проверяет, доступна ли новая версия Ollama в официальном репозитории.

> ⚠️ Пока не обновляет автоматически — только информирует.

## Параметры
Нет. Скрипт работает автономно.

## Что делает
1. **Открывает последнюю страницу релизов** на GitHub:  
   `https://github.com/ollama/ollama/releases/latest`
2. **Ищет ссылку на `ollama-windows-amd64.exe`**
3. **Сравнивает дату последнего изменения** скачиваемого файла с датой локального `ollama.exe`
4. **Выводит результат:**
   - ✅ "Up to date" — если актуально
   - 🆕 "New version available!" — если есть обновление

## Логирование
- Логи сохраняются в: `logs/update_check_YYYY-MM-DD.log`

## Требования
- Подключение к интернету
- Доступ к `github.com`
- Локальный `ollama.exe` в `bin/ollama.exe`

## Как используется
Обычно запускается как **запланированная задача** (например, раз в неделю):
```powershell
Register-ScheduledTask -TaskName "Ollama - Check Update" -Trigger (New-ScheduledTaskTrigger -Weekly -DaysOfWeek Monday -At 9am) -Action (New-ScheduledTaskAction -Execute "PowerShell" -Argument "-File `".\scripts\check_update.ps1`")